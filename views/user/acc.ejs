<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>อาสาสมัครสาธารณสุข หมู่ 8 หนองนกฮูก</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="https://unicons.iconscout.com/release/v4.0.0/css/line.css">
    <link rel="stylesheet" href="/public/css/user/headUser.css"  crossorigin="anonymous">
    <link rel="stylesheet" href="/public/css/user/activityUser.css"  crossorigin="anonymous">
</head>
<body>

    <%- include('head') %>

    <section>
        <div class="topss">
            <div class="box">
                <div class="images">
                    <div class="street">
                        <div class="slider">
                            <div class="images">
                                <input type="radio" name="slide" id="img1">
                                <input type="radio" name="slide" id="img2">
                                <input type="radio" name="slide" id="img3">
                                <input type="radio" name="slide" id="img4">

                                <% Object.values(imgactivity).slice(0, 4).forEach(function(item, index) { %>
                                    <img src="/image/imageAdmin/activityimg/<%= item.imageput1 %>" alt="img<%= index + 1 %>" class="m<%= index + 1 %>">
                                <% }) %>
                            
                                
                            </div>

                            <div class="dots">
                                <% Object.values(imgactivity).slice(0, 4).forEach(function(item, index) { %>
                                    <label for="img<%= index + 1 %>"></label>
                                <% }) %>
                            </div>

                        </div>
                    </div>

                </div>
                <div class="boxs">
                    <div class="boxs">
                
                        <%- include('activimonthuser', {monthData:januaryData, februaryData, marchData, 
                            aprilData, mayData, juneData, julyData, augustData, septemberData, octoberData,
                            novemberData, decemberData }) %>
                        
                    </div> 
                    
                </div>
            </div>
        </div>
    </section>

    
    <!-- <footer>
        <p>อาสาสมัครสาธารณสุขหมู่ที่ 8 หนองนกฮูก</p>
        <p>หมู่ที่ 8 ตำบลมาบโป่ง อำเภอพานทอง จังหวัดชลบุรี รหัสไปรษณี 20160</p>
    </footer> -->

    <script src="/public/js/userJs/activityuser.js"></script>

</body>
</html>